# ==========================================
# Riksdag-Regering MCP Server Configuration
# ==========================================
# API-ONLY MODE: All data is fetched directly from
# Riksdagen (data.riksdagen.se) and Regeringskansliet (g0v.se) APIs
# No database credentials or external services required!

# -----------------
# Server Configuration (för HTTP deployment)
# -----------------
# Server port (default: 3000, Render sets this automatically)
PORT=3000

# Node environment (development, production)
NODE_ENV=development

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Optional: API Key för autentisering (lämna tom för publik access)
# API_KEY=your-secret-api-key-here
